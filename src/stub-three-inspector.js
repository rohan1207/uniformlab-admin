/**
 * Stub for three/addons/inspector/Inspector.js (not present in this three.js version).
 * Used by @react-three/fiber; no-op so the app builds and runs without the dev inspector.
 */
export const Inspector = null;
export default Inspector;

